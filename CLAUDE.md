# Development Guidelines

## Coding Style

* コードは**改行を少なく、簡潔に**書く。
* 数行程度の処理は、可読性を損なわない限り関数化しない。
* 関数化は、処理の再利用性がある場合や、まとまりのある複雑な処理の場合に限定する。
* 各処理ブロックには、その箇所の**役割を説明する日本語コメント**を付ける。
* コメントは「何をしているか」ではなく、基本的に「何のための処理か」が分かるようにする。
* 認知負荷を下げるため、コードは可能な限り単純な構造にする。

## Error Handling

* 認知負荷削減を優先し、エラー処理は極力追加しない。
* 明らかに不要な `try-except` や過剰な入力チェックは避ける。
* エラーは必要に応じて自然に発生させ、原因を追いやすいコードを優先する。

## Build Rule

- exe,installerのビルドは指示したとき以外行わない
